# User data (NEVER commit)
profile.json
resume.txt
resume.pdf
*.env
.env.*
!.env.example

# Runtime artifacts
*.db
tailored_resumes/
cover_letters/
chrome-workers/
apply-workers/
apply_logs/
logs/

# MCP configs (per-machine)
.mcp*.json

# Local envs / tooling artifacts
.venv/
.playwright-mcp/
webui_test.log

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code
.claude/

# JobPilot data home (user-level, must never be committed)
.jobpilot/
# Internal dev coordination note (not part of the product)
_DEV_CONTEXT.md

# Claude brief / notes files (discussion artifacts, not part of the product)
jobpilot-*-brief.md
jobpilot-*-notes.md
# Local dev launcher / logs (not part of the product)
run_web*.py
jobpilot_web.log
jobpilot_web.err
jobpilot_verify.log

# Android companion app build output / debug signing key
android/build/
*.keystore

# PyInstaller / Inno Setup output for the Windows installer
dist/
.build-venv/
installer/__pycache__/
